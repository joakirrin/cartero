Added cartero note to capture session context during development.

---

When no --context-file is provided, cartero commit now uses session notes automatically.

---

If the diff is ambiguous and no notes exist, the commit flow now asks for a short note before falling back.
